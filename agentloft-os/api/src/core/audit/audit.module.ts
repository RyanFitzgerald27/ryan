import { Module } from '@nestjs/common';

// Audit module: emits AuditEvent rows for every mutating action via a Prisma middleware.
// To be built out — see docs/ROADMAP.md.

@Module({})
export class AuditModule {}
